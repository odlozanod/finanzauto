export interface User {
  id: string;
  title?: string;
  firstName: string;
  lastName: string;
  picture?: string;
  email: string;
  gender: string;  
  phone: string;
  dateOfBirth: string;
}
